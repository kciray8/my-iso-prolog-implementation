package iaroslav.baranov.tracklog.service;

import iaroslav.baranov.tracklog.BaseTest;
import iaroslav.baranov.tracklog.ast.term.Term;
import iaroslav.baranov.tracklog.processor.ExecutionContext;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

class TermServiceTest extends BaseTest {
    /*ExecutionContext context = new ExecutionContext();
    TermService termService = new TermService();

    @Test
    void shouldReturnRenamedCopy() {
        Term term = parseTerm("f(A, B, A)");

        Term copy = termService.makeRenamedCopy(term, context);
        Assertions.assertEquals("f(_V0, _V1, _V0)", copy.toCode());
    }*/
}